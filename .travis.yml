language: c

compiler:
  - gcc

branches:
  only:
    - master

before_script:
  - cd /tmp
  - wget ftp://ftp.freeradius.org/pub/radius/freeradius-1.1.8.tar.bz2
  - tar -jxvf freeradius-1.1.8.tar.bz2
  - cd freeradius-1.1.8/src/modules
  - ln -s $TRAVIS_BUILD_DIR rlm_ftress
  - echo "rlm_ftress" >> stable
#  - cd rlm_ftress
#  - ./autogen.sh

script:
  - "cd /tmp/freeradius-1.1.8 && ./configure --prefix=/opt/freeradius-1.1.8 && make && sudo make install"
