TARGET      = @targetname@
SRCS        = rlm_ftress.c client.c
HEADERS     = ftress.h
RLM_CFLAGS  = @ftress_cflags@
RLM_LIBS    = @ftress_ldflags@ -L ./ -lftress -lssl -lcrypto -lfreeradius-client
RLM_INSTALL = install-example

include ../rules.mak

$(LT_OBJS): $(HEADERS)

install-example:
	touch .
